---
deployment:
  tasks:
    # One command: rsync + conditional composer. The old per-line
    # `cp -R *` / `chmod -R storage` / `composer install` pipeline is why
    # cPanel sat on "deployment in progress" with no progress bar.
    - export DEPLOYPATH=/home/voxsignco/pearledu-app; /bin/bash ./scripts/cpanel-deploy.sh
